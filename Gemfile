source 'http://rubygems.org'

gem 'rails', '3.0.6'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem "mysql2"
gem "mysql"
gem "thin"
gem "haml"
gem "hpricot"
gem "net-ldap"
gem "win32console", :require => 'win32/console/ansi', :platform => [:mingw]

gem "yaih", :group => :irb, :platform => [:mingw]
gem "wirble", :group => :irb, :platform => [:mingw]
gem "map_by_method", :group => :irb, :platform => [:mingw]
gem "sketches", :group => :irb, :platform => [:mingw]
gem "hirb", :group => :irb, :platform => [:mingw]

gem "net-ssh"
gem "net-sftp"

gem "exception_notification", :require => 'exception_notifier'

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
